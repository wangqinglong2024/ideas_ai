# 99 · 待确认问题清单

> 引用：[00-index.md](./00-index.md)
> 用途：AI 在生成本规范时不确定、需 PM（用户）拍板的事项。
> **答复方式**：在每行的"PM 决策"列直接填写结论；AI 收到后即更新对应规则文件并清空本行。

---

| 编号 | 问题 | AI 默认方案 | 备选 | 影响范围 | PM 决策 |
|------|------|------------|------|---------|--------|
| Q1 | 用户偏好"Node.js + JS"，但 LangGraph、Drizzle、Zod 在 TS 下心智成本明显更低；是否允许 `packages/db`、`packages/shared-schemas`、AI 工作流模块**例外使用 TS**？ | 默认允许上述 3 处例外，其余后端均 JS + JSDoc | (a) 全部强制 JS；(b) 全部改 TS | 01-技术栈、05-编码规范 | |
答案：全部TS，LangGraph本身就有TS版本，我必须确保后期代码可人工维护；
| Q2 | 后端是用 **Express 4** 还是 **Fastify 4 / Hono**？用户未指定。 | Express 4（生态最稳、与 Supabase 文档示例一致） | Fastify（性能更好）/ Hono（新潮、edge 友好）| 01-技术栈、05-编码规范 | |
答案：Hono
| Q3 | 是否使用 **Drizzle ORM**？用户只指定 Supabase Postgres，未指定 ORM。 | 使用 Drizzle（轻量、SQL-near、用户记忆中已踩过坑） | 直接 supabase-js / Prisma / Kysely | 03-数据库规范、05-编码规范 | |
答案：直接用supabase处理数据库；MCP模式；
| Q4 | 包管理器：**pnpm** 还是 npm/yarn？monorepo 偏好 pnpm workspaces。 | pnpm 9 + workspaces | npm workspaces / yarn berry | 02-项目目录结构、06-部署 | |
答案：pnpm
| Q5 | 应用端是否需要 **PWA / 离线学习** 能力？东南亚网络波动较大。 | v1 不做，仅响应式；v2 评估 | v1 直接做 PWA + 课程缓存 | 01-技术栈、07-国际化与响应式 | |
答案：不仅不做，还要做动态渲染；不要把数据嵌入HTML页，反爬虫!
| Q6 | 应用端是否需要打包 **原生 App**（React Native / Capacitor）？用户原文是"软件"。 | v1 仅 Web（响应式覆盖移动端浏览器）；原生延后 | 直接 Capacitor 包壳 / RN 重写 | 01-技术栈、02-目录结构 | |
答案：仅Web响应式，不需要做App；后期再搞React Native
| Q7 | 反向代理用 **Caddy** 还是 Nginx？dev 环境简化 HTTPS 配置。 | Caddy（自动 TLS、配置极简） | Nginx | 06-部署与环境 | |
答案：Nginx？dev直接用端口访问，我会自行处理后期部署HTTPS问题；
| Q8 | LangGraph 官方 **TS Runtime 仍 beta**，是否接受 beta？或 LangGraph 改为通过 Python 子镜像调用？ | 接受 LangGraph TS beta，封装在 `packages/ai-adapters/workflow/`；不可用时降级 Vercel AI SDK | 增加 `langgraph-py` 容器走 HTTP | 01-技术栈、AI 工作流相关 PRD |  |
答案：接受beta，必须用TS版本；
| Q9 | 微信支付仅适合中国用户，**东南亚用户**真实支付通道是什么？（Stripe / 本地钱包 MoMo / TrueMoney / DANA / GrabPay） | 默认接 Stripe（国际卡）+ 预留本地钱包 Adapter，缺 Key 全 mock | 仅微信支付（不合理）/ 直接接全量本地钱包 | 06-部署、PRD 10-payment | |
答案：用Paddle，前期模拟mock即可
| Q10 | 是否需要 **后端 Edge Functions**（Supabase Edge）？目前架构已用 Express，可能重复。 | 仅作为第三方 webhook 边缘转发；业务逻辑全走 Express | 全栈搬到 Edge / 完全不用 | 01-技术栈、06-部署 | |
答案：这个你决定，应该需要，因为我改Hono了，再有Spabase还要处理鉴权等问题；
| Q11 | 数据库 schema 名拟定 `zhiyu_dev`，是否符合命名习惯？ | `zhiyu_dev` | `public` / `zhiyu` | 03-数据库规范、06-部署 | |
数据库命名你自己定就行；
| Q12 | 国际化文案 5 语言由谁产出？AI 自动翻译占位 + 后续人工校对，还是必须人工先行？ | AI 自动翻译占位（标 `needs_review: true`），上线前 PM 安排校对 | 必须人工先行 / 仅做中英 | 07-国际化与响应式 | |
答案：AI 自动翻译占位
| Q13 | 应用端是否需要 **第三方登录**（Google、Facebook、Line、Apple、微信）？地区差异大。 | 默认开 Google + Apple + 邮箱 + 手机号；其余按地区扩展 | 仅邮箱 + 手机号 | PRD 06-user-account | |
答案：Google登录+邮箱登录，都是Mock就好了
| Q14 | 字体（尤其泰文 / 越文）是否允许走 Google Fonts CDN？东南亚部分地区有访问问题。 | 默认自托管到 Supabase Storage（已写入 07） | Google Fonts 直链 | 07-国际化与响应式 | |
答案：你注意，我的Supabase是本地化的哦，不是官网的。然后字体我要求好看，其他的你决定。
---

> 当列表全部清空时，请删除本文件并在 [00-index.md](./00-index.md) 移除链接。

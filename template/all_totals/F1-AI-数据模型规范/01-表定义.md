# 一 · 表定义（模板）

> 单文件超过 1200 行时按表拆为 `01-表定义-<表名>.md`，并在 [00-index.md](./00-index.md) 更新导航。

---

## 表：`[表名]`

**对应需求中的对象**：`[F1-用户中的中文对象名]`
**性质**：`[字典表 / CRUD 内容表 / 用户数据表]`，是否启用软删，是否走 RLS。

### 一、字段定义

| 字段名 | 类型 | 必填 | 默认值 | 索引 | 对应需求中的信息 | 校验规则 |
|--------|------|------|--------|------|----------------|---------|
| | | | | | | |

### 二、AI 补充字段（需求中未提及，技术必要）

| 字段名 | 类型 | 补充理由 |
|--------|------|---------|
| | | |

### 三、DDL 草案

```sql
-- create table ...
```

### 四、RLS 策略（如启用）

| 操作 | 角色 | 策略 |
|------|------|------|
| select | | |
| insert/update/delete | | |

策略命名：`<表名>_<操作>_<对象>`。

---

## 表：`[第二张表，如有]`

`[重复上面的格式]`

# 八 · 种子数据（模板）

> 至少覆盖：每种枚举值各 1 条、典型业务流程的最小可演示数据。

---

## 表：`[表名]`

```sql
insert into <schema>.<table> (...) values
  (...),
  (...);
```

或 JSON 形式：

```json
[
  { /* 状态A 示例 */ },
  { /* 状态B 示例 */ }
]
```

种子加载位置：`system/supabase/seed.sql` 或 `system/scripts/db/seed/<功能>.ts`。

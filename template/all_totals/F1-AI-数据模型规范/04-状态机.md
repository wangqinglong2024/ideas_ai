# 四 · 状态机（模板）

> 没有状态机的功能可保留本文件并写"无"；不要删除以保持目录一致性。

## 状态转换图

```
[AI 用文本图画出状态转换]
```

---

## 状态转换规则

| 从 | 到 | 对应需求 | 校验条件 | 字段变更 |
|----|---|---------|---------|---------|
| `[枚举值]` | `[枚举值]` | `[引用 F1-用户中的哪一行]` | `[必须满足什么条件]` | `[哪些字段会被更新]` |

---

## 禁止的转换

```
[列出所有不允许的状态变化，后端必须校验]
```

---

## 实现位置

- RPC：`fn_xxx_transition(p_id uuid, ...)`
- 校验由 RPC 内部完成；前端仅触发，**绝不信任前端的 status 直接写入**
- 前端按钮根据当前状态切换显示文本

---

## 触发副作用

| 转换 | 副作用 |
|------|--------|
| | |

# 六 · 计算规则（模板）

> 把 PM 的自然语言计算规则翻译为精确的公式和实现方案。无则保留本文件并标注"无"以保持目录一致性。

---

## 字段：`[字段名]`

```
公式：[字段名] = [明确的算式]

存储精度：[以分为单位的整数 / decimal(10,2) / 其它]
显示格式：[¥XX.XX / 其它]

边界情况：
- 除数为 0 时：[抛错码 / 返回 null / 取 0]
- 金额溢出时：[抛错码 / 截断 / clamp]
- 负数：[允许 / 禁止]
- 时区：[业务时区 / UTC]
```

实现位置：`[数据库生成列 / RPC / 应用层 service]`

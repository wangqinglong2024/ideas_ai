# 三 · 表关系（模板）

## ER 概览

```
[精确描述表之间的关系，包括外键和删除级联策略]

[表A] 1 ────< N [表B]   （通过字段 xxx 关联）
  删除策略：[CASCADE / RESTRICT / SET NULL]

[其他关系...]
```

---

## 关系明细

| 父表 | 子表 | 外键字段 | 删除策略 | 理由 |
|------|------|---------|---------|------|
| | | | | |

---

## 软删与级联的协同

- `xxx.deleted_at IS NOT NULL` → 列表与详情接口默认过滤
- 是否支持恢复：`[是 / 否]`
- 物理清理策略：`[cron 任务名] / [保留窗口]`

## 跨域副作用

- 列出本域的状态变更/删除会引发哪些其它域的副作用（清缓存、清进度、触发任务等）
- 实现位置：建议放在 RPC `fn_xxx` 内事务调用

## 基数约束

- 列出"父记录至少 / 至多有几条子记录"等业务规则；不写成数据库 CHECK 时，注明在哪个 RPC 内强制

# 二 · AI 补充接口（模板）

> 需求中没有明确提到，但技术上需要的接口（健康检查、内部回调、缓存预热、Mock 等）。

| 接口名称 | 用途 | 补充理由 | 详细定义位置 |
|---------|------|---------|--------------|
| `GET /api/v1/<功能>/health` | 健康检查 | 标准 200，供 LB / 监控调用 | 本文件 §1 |
| | | | |

---

## §1 健康检查（示例）

**方法**：GET
**路径**：`/api/v1/<功能>/health`
**权限**：公开

**成功响应 200**：

```json
{ "code": 0, "message": "ok", "data": { "status": "healthy" } }
```

# 一 · 接口详细定义（模板）

> 单文件超过 1200 行时按功能子模块拆为：`01-应用端-<子模块>.md`、`01-管理端-<子模块>.md` 等，并在 [00-index.md](./00-index.md) 更新导航。

---

## [接口编号] · [接口名称]

**对应需求中的操作**：`[F2-用户中的第几项操作]`

**方法**：`[GET / POST / PUT / PATCH / DELETE]`
**路径**：`[遵循 G1 URL 命名规则]`
**权限**：`[公开 / 登录用户 / 管理员 / 分级公开 …，引用 F2-用户中的权限定义]`

**业务校验**（在参数校验之外的业务逻辑校验）：

```
- 例：订单状态必须为 pending_confirm 才能调用此接口
- 例：当前用户必须有该操作权限
```

**请求参数**：

| 参数名 | 位置 | 类型 | 必填 | 说明 | 校验规则（引用 F1-AI） |
|--------|------|------|------|------|---------------------|
| | | | | | |

**成功响应 200**：

```json
{
  "code": 0,
  "message": "ok",
  "data": { }
}
```

**错误场景**：

| HTTP | code | 触发条件 | 错误信息 |
|------|------|---------|---------|
| 400 | | 参数校验失败 | |
| 401 | | 未登录 | |
| 403 | | 无权限 | |
| 404 | | 资源不存在 | |
| 409 | | 状态冲突（如订单已被处理） | |

---

## [接口编号] · [下一个接口]

`[重复上面的完整格式]`

# F2-用户：操作与业务逻辑

**提示词**：`"请根据我的技术偏好，
按照F2-AI模板的格式，/opt/projects/zhiyu/template/all_totals/F2-AI-接口规范.md
生成完整的架构与技术规范。不确定的地方列在待确认问题清单中。"`

必须严格按照模板内容和我的偏好输出，输出位置为/opt/projects/zhiyu/function/01-china/ai;
禁止你在创建或更新文件时，单文件内容超过1200行，如果文件过大，必须将原文件拆分为多个，放在一个文件夹下。
有你不明确的内容时，必须向我提问，但是不要终止当前对话。没有完成你要做事的事情之前，禁止你自行终止。

再就是你输出的时候，要把当前F2-AI模板的内容按照你新生成的结果，拆分为多个子文件，便于未来AI可以快速定位查找具体规范。

要符合：符合/opt/projects/zhiyu/grules/G1-架构与技术规范/04-API接口规范.md
---

## 功能名称：`[填写，与F1一致]`

---

## 一、这个功能需要哪些操作

> 用大白话列出用户在这个功能里能做哪些事情。不用写接口路径，只说"做什么"。

```

应用端：
用户端进入Tab后，是12个类目，每个类目的名称及介绍。这里的展示文字是多语言的，系统自动适配多语言。

进入后是对应类目下的文章列表；显示中文的文章标题和对应语言的文章标题；拼音、中文、文章标题。

逐句查看文章，每个句子一个卡片。卡片展示如下：拼音最上方，然后中文，然后本地语言。这个本地语言就是用户设置系统语言是什么，这里就显示那个语言。如果是中文，则只保留拼音、中文。

朗读：每个句子都有一个语音播放按钮，调AI接口播放并缓存。只要全平台有一个用户播放，就把播放数据缓存下来。
全文朗读，用户可点击全文朗读，逐个句子读出来。


管理端
发现中国菜单进入后，是12个类目卡片，点击某个卡片后，进入类目下的文章（带分页）
选择每个文章后，进入文章下的句子。点击编辑按钮，进入编辑状态，最上方是文章基本信息，下方是文章的多个句子，句子带分页。每个句子支持多语言编辑（5种），文章名称也支持多语言编辑（5种）。
可在开头，结尾或某个句子中间，添加一个句子；添加后，就多个卡片，卡片5个Tab切换语言用来。
文章列表页要显示当前类目的文章总数，句子列表页要展示当前页面中文句子的总数。
文章ID+句子ID。是句子的唯一ID，该ID对应唯一一个永久缓存的语音，就是掉大模型拿到的。
调大模型功能暂时Mock；
```


---

## 二、每个操作谁能用

管理员有管理端所有权限
用户有用户端所有权限。
---

## 三、操作的特殊规则

> 某些操作有条件限制或特殊逻辑时写在这里。

```
文章可查看、编辑、删除、新增，变更后应用端刷新时生效。

```

---

## 四、边界情况

> 写你能想到的"如果...怎么办"。

如果未来管理员账号增加，多人同时编辑一个文章，以后保存的为准。

---

## 五、补充说明

其他我没想到的你可以补充。

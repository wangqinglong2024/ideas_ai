# 12 · F2 已封板决策清单

> 本文件保留原编号，用于承接历史引用；当前 F2 接口规范决策已封板。

---

| 编号 | 决策 | 接口影响 |
|---|---|---|
| Q1 | 学员侧读写以 RESTful 路径表达 | 读侧可走 RLS 视图，写侧走业务 RPC，前端不感知内部实现。 |
| Q2 | 考试类型固定为章测、阶段考、HSK 模考；节测走同一考试配置模型 | P-A-8 统一维护节测、章测、阶段考。 |
| Q3 | SRS 今日队列上限 50 | 超出返回限额提示，不额外生成个性化内容。 |
| Q7 | 管理端搜索按 5 语言内容字段检索 | 不支持拼音搜索；支持中文、外文、编码、题面摘要。 |
| Q8 | 错题本本期只读 | 学员通过 SRS 队列自然复习。 |
| Q9 | 媒资删除先软删再延迟清理 CDN | 默认 7 天保留窗口。 |
| Q11 | 限流按学员端为主，管理端业务不限流 | 管理端保留网关级基础防护。 |
| Q13 | 当前主题写入用户设置 | 学员切换主题后同步保存 `current_track`。 |

---

## 发布与举报接口封板

- 发布、下架统一通过 `A16` 切换 `is_published`。
- 学员举报统一通过 `A18/A19` 列表和处理。
- 内容导入统一通过 `A15` 预览、确认导入和追溯批次。

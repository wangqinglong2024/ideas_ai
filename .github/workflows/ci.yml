name: docker-ci

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  docker-verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build and verify inside Docker
        run: docker compose -f docker-compose.test.yml run --rm --build test

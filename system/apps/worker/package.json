{
  "name": "@zhiyu/worker",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tsx src/worker.ts",
    "build": "tsup src/worker.ts --format esm --out-dir dist",
    "start": "node dist/worker.js",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@zhiyu/backend": "workspace:*",
    "tsx": "^4.19.2"
  },
  "devDependencies": {
    "tsup": "^8.3.5",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}